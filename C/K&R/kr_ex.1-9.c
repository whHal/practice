#include <stdio.h>

int main() 
{
  int c, blank; 
  blank = ' ';
  
  while ((c = getchar()) != EOF) {
    if (c == ' ') {
    		printf("%c\n", blank);
	} else if (c != ' ') {
		
    	  }
    	}
  return 0;
}
